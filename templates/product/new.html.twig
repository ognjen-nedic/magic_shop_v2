<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>New Product</title>
</head>
<body>
    <h1 class="text-center">Ogara's Magic Shop</h1>
    <h2 class="text-center">Add new product</h2>
    <form action = "{{ path('new_product') }}" method="POST" >
        <div class="mx-auto d-flex flex-column justify-content-between" style="gap:10px; width: 200px">

        <input class="btn btn-outline-secondary" type="text" name="product_name" placeholder="Product name"/>
        <select class="btn btn-outline-secondary" name="type">
            {% for type in types %}
                <option value={{ type.getId() }}> {{ type.getTypeName() }} </option>
            {% endfor %}
        </select >
        <select class="btn btn-outline-secondary" name="rarity">
            {% for rarity in rarities %}
                <option value={{ rarity.getId() }}> {{ rarity.getRarityName() }} </option>
            {% endfor %}
        </select>        
        <input class="btn btn-outline-secondary" type="number" name="product_price" placeholder="Product price" />
        <input class="btn btn-outline-success" type="submit" name="form_submit" />
        <a class="btn btn-outline-danger" href="{{ path('list_products') }}">Cancel</a>
        </div>
    </form>
</body>
</html>